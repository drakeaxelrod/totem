# Dongle is always-on (USB or battery powered) — never idle or sleep
CONFIG_ZMK_IDLE_TIMEOUT=86400000
CONFIG_ZMK_SLEEP=n

# Max TX power for reliable BLE to both peripherals
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# ZMK Studio over USB
CONFIG_ZMK_STUDIO=y

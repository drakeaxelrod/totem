# TOTEM dongle (USB receiver) — central for both halves

# Device identity
CONFIG_USB_DEVICE_MANUFACTURER="GEIGEIGEIST"
CONFIG_USB_DEVICE_PRODUCT="TOTEM Keyboard"
CONFIG_BT_DIS_MANUF_NAME_STR="GEIGEIGEIST"

# General
CONFIG_ZMK_USB_BOOT=y

# ZMK Studio (runtime keymap editing)
CONFIG_ZMK_STUDIO=y

# Battery saving (dongle is USB-powered, but still useful for BLE idle)
CONFIG_ZMK_IDLE_TIMEOUT=30000

# Mouse keys / pointing
CONFIG_ZMK_POINTING=y
